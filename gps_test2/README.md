# GPS_Test2

## Project Description

Wir haben einen ESP32-S3 Dieser hat einen TFT. Mit #include <CYD_Display_Config.h> wird die LGFX Klasse für den Schirm und Touchscreen initialisiert und definiert, unter Verwendung der der LovyanGFX (Das zugehörige Objekt musst du im Code generieren (z.B. LGFX lcd;)
Angeschlossen auch ein Quectel_L80-R GPS der am Pin 8 mit 9600 Baud empfangen wird. 
Mache, dass das Board dafür einen sinnvollen Info-screen ausgibt und den laufend aktualisiert